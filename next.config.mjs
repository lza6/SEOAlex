/** @type {import('next').NextConfig} */
const nextConfig = {
    /* config options here */
    experimental: {
        // turbopack: { root: '.' } // 可以在此手动指定根目录
    }
};

export default nextConfig;
